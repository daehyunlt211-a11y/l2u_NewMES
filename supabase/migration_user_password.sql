-- =====================================================================
-- 사용자 로그인 비밀번호 컬럼 추가 (기존 프로젝트용)
-- schema.sql 을 이미 실행한 프로젝트에서 이 파일만 추가 실행하세요.
-- =====================================================================

alter table users add column if not exists password text;

-- (선택) 데모 계정 비밀번호 일괄 설정 — 운영에서는 사용하지 마세요.
-- update users set password = '1234' where password is null;

-- ⚠️ 보안 주의: 평문 비밀번호 저장은 개발/데모 단계에서만 사용하세요.
--    운영 환경에서는 Supabase Auth 또는 해시(bcrypt 등)를 사용해야 합니다.
